name: CI — Code Quality & Guardrails

on:
  pull_request:
    branches: [ main, staging ]
  push:
    branches: [ main, staging ]
  workflow_dispatch:

jobs:
  reuse:
    uses: rook-system-agentic/.github/.github/workflows/node-ci.yml@main
    with:
      node_version: '20'
      cache_manager: 'pnpm'
